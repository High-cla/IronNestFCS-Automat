# Daily Report — 2026-08-02

## v1.1.0 Release 准备
- 提交 TacticalRadar.cs Entity 地下属性反射检测（IsUnderground/Underground/IsBunker/Bunker）
- 版本号 1.0.5 → 1.1.0，作者更新为 KK
- 创建 release zip（Mods/ + UserData/IronNestFCS/ + UserLibs/），结构对齐原版 v1.0.5
- 新 README：中英双语，覆盖战术智能 6 项新特性 + 架构/构建/使用说明
- `release/` 加入 .gitignore，打 v1.1.0 tag

## Bug 修复
- **FDC 弹种错误**：FDC 目标的 Icon 含 "fire direction" 但 `isArmored` 未检测，导致打 HE 而非 AP。在 `isArmored` 判断中添加 `icon.IndexOf("fire direction")` 检查。

## 日志清理
- 删除 `RunTelemetryScan()`（~150 行，每次启动 dump 几百行 Component 属性）
- 删除 `DumpGameObjectRecursive()`（递归 dump 子树）
- 删除 `TacticalRadar` 中 Entity 属性一次性 dump
- CBT poll 改为仅在激活/结束时各打一行，不再每秒输出 "0 timers"
- 禁用探针调用（保留代码供后续调试）

## 雷达数据源重构（核心工作）
### 背景
无尽模式下第二波 CBT 的 FDC 目标不在 `FireMissionRoot.children` 中，导致雷达无法发现新 spawn 的 FDC。

### 探索过程
1. **Il2Cpp Dictionary 深探**：确认 `FireMission.Entities` = `Dictionary<string, MapEntity>`（40 条，全量目标注册表），`RunningTimers` = `Dictionary<string, TimerValue>`
2. **MapEntity 属性确认**：`ID, Name, Icon, Role, Position, State, MaxHealth, Armour, Stars, Scale, ImmuneShells, Location, Health, IsAlive` — 与 Entity 结构完全一致
3. **坐标系统问题**：`MapEntity.Position` 是游戏公里网格坐标，不在 Draggable Surface 本地空间。`TransformPoint` 直接使用时坐标错误（打到 off-map）
4. **解决方案**：`FireMission.coordinateRoot`（RectTransform, 20×10, pivot=0,0, parent='MapRoot'）是网格坐标→世界坐标的桥梁。`coordinateRoot.TransformPoint(MapEntity.Position)` 给出正确的世界坐标

### 最终架构
- **数据源**：`FireMission.Entities` Dictionary → 枚举全部 40 条 MapEntity
- **存活过滤**：`IsAlive == true`
- **坐标**：优先 `MapEntity.Location.transform.position`（已 spawn），fallback `coordinateRoot.TransformPoint(MapEntity.Position)`（未 spawn 但位置已设定）
- **去重**：从 `ChildIndex`（脆弱的 int 索引）改为 `EntityId`（MapEntity key 字符串）
- **装甲判断**：直接使用 `MapEntity.Armour > 0`，不再仅靠 Role/Icon 推测

## 待验证
- CBT 第二波 FDC spawn 时，`IsAlive=true` 但 `Location=null` 的 MapEntity 能否通过 `coordinateRoot.TransformPoint` 获得正确瞄准坐标
- `MapEntity.State` 字段（MapEntityStates 枚举）的实际值变化——可能用于检测 spawn 事件

## 提交统计
17 commits，净变更：-176 行旧代码（日志噪音/EntityLocation 反射），+~300 行新代码（Dictionary 枚举/坐标桥接/EntityId 去重）
