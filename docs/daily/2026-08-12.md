# Daily Report — 2026-08-12

> 本机无 .NET SDK（仅 10.0.9 runtime）且无游戏，**以下所有代码改动未编译、未测试**。
> 本文档供有构建环境的机器上的 agent 接手：先读懂改动，再编译 + 按验证清单测试。

## 1. issue #3 计划收尾（已提交 d906378）

- `docs/superpowers/plans/2026-08-10-human-machine-collaboration-registry.md` 全部任务勾选完成
- 验证注记更新：Tasks 1–8 已提交，场景 A–F 已于 2026-08-11 在有编译环境的机器跑通

## 2. 新特性：面板预计落弹（Impact Countdown）

### 需求

射表解算出 → 面板显示预计落弹；开火瞬间开始倒计时；落地（射表估计）归零。

### 改动文件

| 文件 | 改动 |
|---|---|
| `IronNestFCS.Logic/FCS/ToFTable.cs`（新） | 6 档装药 × 3–20km 飞行时间表（数据源 `ToF.txt`），线性插值 + 越界夹紧，纯数据无 IL2CPP 引用 |
| `IronNestFCS.Logic/FCS/ArtilleryTask.cs` | +`EstimatedToF`（解算后置位）、+`FiredAt`（开火时置位） |
| `IronNestFCS.Logic/FSC.cs` | 解算后 `GetElevation()` 处算 `EstimatedToF`；开火时记 `FiredAt` + `InFlight.Add`；`Update()` 归零移出；`Dispose` 清空 |
| `IronNestFCS.Logic/FcsWindow.cs` | 炮位行新增 `Impact` 行；新增 `In flight` 区显示已击发任务倒计时 |

### 设计要点（接手时必读）

- **`FSC.InFlight` 列表**：开火后任务只占炮位 ~13s（退膛恢复），而飞行时间最长 ~39s。倒计时若只挂炮位行会在落地前消失，所以开火时把任务加入 `InFlight`，退膛（`Finished`）后继续在 `In flight` 区倒数；`Time.time - FiredAt >= EstimatedToF` 时由 `Update()` 移除。
- **`In flight` 区过滤 `t.progress == Progress.Finished`**：避免与炮位行（BackToIdle 阶段）重复显示。
- **`EstimatedToF` 写入时机**：解算完成（`GetElevation()` 后）才写入，满足"从射表算出来那一步开始显示"。装药档 `powderCount` 在解算前已决定（`MinimumCharge` 或满装 6），查表用 `powderCount` 天然覆盖 maxCharge 情况。
- **倒计时 = 射表估计**，未接击杀确认（`Reconcile`）提前归零。

### ToF 射表数据（`ToFTable.FlightSeconds`）

来源：`ToF.txt`（仓库根，**未提交**，测量源数据）。每行 = 射程 3,4,5,…,20km 的飞行秒数；远端缺数据用该档最远值夹紧：

```
装药1: 14, 19, 23, 然后全 23            （该档最远 5km）
装药2: 11, 15, 19, 23, 26.5, 30, 34, 38, 然后全 38   （最远 10km）
装药3: 7, 10, 13, 15, 18, 20, 22.5, 26, 28, 31, 34, 36.5, 39, 然后全 39  （最远 15km）
装药4: 5, 7, 9, 11, 13, 15, 17, 18.5, 20, 22, 24, 26, 28, 30, 32, 34, 35.5, 37
装药5: 4, 5.5, 7, 9, 11, 12.5, 13.5, 15, 16, 18, 20, 21, 23, 24, 26, 27, 29, 30
装药6: 4, 5, 7, 8, 10, 11, 12, 14, 15, 17, 18, 20, 21, 22, 24, 26, 27, 28
```

> 数据是从 ToF.txt 手工录入代码的，**未经验证**。接手时抽查 2–3 个单元与 ToF.txt 对照，必要时修正 `ToFTable.FlightSeconds`。

### 构建 + 运行时验证清单（构建机）

1. `.\build.ps1`（GameDirectory 默认指向正式版游戏目录）→ BUILD SUCCEEDED
2. **G1 面板显示**：任意自动任务解算完成后，炮位行出现 `Impact: ~Xs`，数值与 ToFTable 按距离插值一致（抽查 2–3 个距离）
3. **G2 开火倒计时**：击发瞬间变 `Impact: Ns` 持续减小；任务退膛后转入 `In flight` 区继续倒数
4. **G3 落地归零**：倒计时到 0 后 `In flight` 条目消失；与实测落弹时刻比对，误差应落在射表精度内
5. **G4 手动开火**：手动任务同样有 Impact 显示（手动/自动共用 `RunTaskRoutine` 路径）
6. **G5 热重载回归**：F9 重载无残留（`InFlight` 已 `Clear`）、无协程泄漏

### 已知简化与边界

- 倒计时未接击杀确认（目标实际早死时不会提前归零）
- 射表仅覆盖 3–20km，更近/更远夹边界值（估算可能偏）
- 射表只有"装药 × 射程"两个维度，无弹种维度
- 本机未编译，以上全部待构建机确认

## 3. 移动目标跟踪（方案 2 统一路径）— 实现完成，未编译未测试

> 设计文档 `docs/moving-target-tracking.md`；实施计划 `docs/superpowers/plans/2026-08-12-moving-target-tracking.md`。
> **本机无 .NET SDK（仅 10.0.9 runtime）且无游戏 → 所有改动未编译未测试。编译 + 运行时验证今晚在构建机做（Task 8）。**

### 已实现（Tasks 1-7，代码在工作区）

| 文件 | 改动 |
|---|---|
| `IronNestFCS.Logic/FCS/TargetLeadSolver.cs`（新） | aim(t) 闭合式：`LeadPoint`/`ChargeFor`/`Elevation`/`IsMoving`（匀速假设，纯数学） |
| `IronNestFCS.Logic/TacticalRadar.cs` | 速度估计改 4-5s 基线（`moveHistory` 队列首尾差分，比 1s 单帧差分平滑） |
| `IronNestFCS.Logic/FCS/ArtilleryTask.cs` | +`IsMoving`/`LoadedCharge`/`AimP0`/`AimVel`/`AimStartTime`（冻结快照） |
| `IronNestFCS.Logic/FCS/Turret.cs` | +`SetDesiredRotation`/`AngleError`（非阻塞 + 就绪判定） |
| `IronNestFCS.Logic/FCS/GunSystem.cs` | +`SetElevationTarget`/`ElevationError`/`IsPendingReload` |
| `IronNestFCS.Logic/FSC.cs` | 统一路径重构：解算移出 desk 锁；装药按预测最远距离定格；连续瞄准跟踪（收敛+复检+AutoFire 闸门）；计算器纯装饰并行同步；后台装填期追方位；退化路径 |
| `IronNestFCS.Logic/FcsModule.cs` | 自动任务创建拷冻结快照（手动任务 `IsMoving=false` 默认静态） |
| `IronNestFCS.Logic/FcsWindow.cs` | 炮位行 `[MOV]` 标记 |

### 核心语义

- **aim(t) = AimP0 + AimVel×(t − AimStartTime + ToF)**，冻结快照外推，全程不查雷达（匀速假设）
- 仰角公式 `0.012 × dist_m ÷ LoadedCharge`（装药装填时定格，覆盖预测最远距离）
- 开火条件 = 收敛检查（|机构−aim|<1° 且 CanFire）→ 确认序列 → 复检对齐 → AutoFire 闸门

### 构建机验证清单（今晚）

1. `.\build.ps1` → BUILD SUCCEEDED
2. **公式对拍（静态）**：静态目标若干距离，装饰驱动计算器时顺带读回输出，log 公式仰角 vs 计算器读数，误差 < 1°
3. **跳过计算器冒烟**：直接设仰角杆（不碰计算器），5 步确认 + 击发正常、落点正确（已确认允许）
4. **静态目标回归**：静态目标正常击发、落点正确（公式 vs 原计算器路径等效）
5. **移动目标（匀速直线）**：装填期方向机跟随 → 装填后仰角跟随 → 击发命中提前点，`killed in flight` 出现、注册表 5s 内释放
6. **双发防重**：炮 A 打移动目标期间，炮 B `PickTarget` 不选同一 entityId
7. **退化**：移动目标加速出射程 → Failed/中止无卡死；中途失速 → 静态退化正常击发
8. **计算器装饰**：每发装填期拉一次（视觉正常），不回传、不参与瞄准
9. **热重载回归**：F9 无残留、无协程泄漏

### 已知注意点

- `EstimatedToF`（面板倒计时）对移动任务用创建时距离估——轻微低估远飞时间，近似可接受
- 手动等待（AutoFire 关 + 移动目标）无超时，等玩家扳机（与原 `WaitFire` 语义一致）
- aim 循环/复检超时已用**时间制**（帧数在 60fps 下 240 帧仅 4s，会误杀 32s 仰角摆动——已修）
- `AngleError` 依赖 `rotationVelocity==0` 门控（未转完返回 180°，避免"刚设拨盘就判对齐"）
- 单位约定：位置=世界单位、距离=km（×3.8164）、速度=世界单位/s——注释已标，严格一致
