# 协同模式（CO-OP）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新增协同模式：自动脚本（雷达）只占用右炮管，左炮管永远留给玩家标点开火。玩家与程序一左一右并行作战，开火序列稳态下自然交替（P→A→P→A）。

**前置依赖（硬依赖，必须先落地）:** `docs/superpowers/plans/2026-08-10-human-machine-collaboration-registry.md`（issue #3a 注册表 + #3b 切割点）：
- `ArtilleryTask.Source`（Auto/Manual）→ `CanUseBarrel` 炮管匹配的唯一依据
- `Registry.Commit`（手动入队即登记）+ `IsHandled` 过滤 PickTarget → 雷达不抢玩家已标目标
- 3b `Canceled` 切割点 → Auto→Coop 时左管自动任务干净让位
- `ClearPendingTasks` 保手动 → 雷达 5s 清队列不冲掉玩家排队任务

**Architecture:** 模式状态机下沉到 `FSC`（`TryDispatch` 需要它做炮管匹配），`FcsModule` 只做编排（雷达启停 / 输入转发 / 面板同步），3D 世界按钮做模式切换（ClickRaycaster 路径），numpad/手柄保留为快捷键。注册表零改动。

**Tech Stack:** .NET 6 / MelonLoader (IL2CPP) / UnityEngine InputSystem。无测试框架（IL2CPP 运行时行为），验证 = 编译 + 运行时清单（见 Task 5）。

---

## 背景与设计决策（已与仓库主确认）

### 模式矩阵

| 模式 | 雷达 | 雷达占用炮管 | 玩家可用炮管 |
|---|---|---|---|
| AUTO（现状）| 激活 | 双管 | 无（手动开火进共享队列竞争）|
| **CO-OP（新）** | 激活 | **仅右管** | **仅左管** |
| MANUAL | 休眠 | 无 | 双管 |

### 决策表

| 决策点 | 结论 |
|---|---|
| 切换入口 | 3D 世界按钮（AutoFire/MaxCharge 同排，走已验证的 ClickRaycaster）+ Numpad 0 / Ctrl+0 / 手柄 Select 循环 `Auto→Coop→Manual→Auto` |
| numpad 去留 | 保留为快捷键（面板为主，不破坏肌肉记忆）|
| 炮塔竞争 | **排队等待**（不抢占）。每自动循环"击发完成→退膛结束"（~13s）炮塔锁空闲——玩家在这期间开火立即拿到；中途开火最多等一个射循环（~30s），无饥饿 |
| 开火序列 | 稳态 **P→A→P→A 交替**，为锁持有期+槽位释放期的自然涌现行为，无需轮换代码；紧相位偶发双发且自校正；单侧空闲时连续发射属正常 |
| 标记所有权 | **CO-OP 下 `radar.AutoPlaceMarkers = false`**（标记是玩家的操作界面，雷达只在纯 AUTO 模式占用它们；否则每 5s 覆盖玩家标记导致手动标点失效）|
| 面板显示 | `[AUTO]/[COOP]/[MANUAL]`；COOP 下炮管行标 `L:Player` / `R:Auto`（玩家能看出"炮管等炮塔"是正常状态）|
| 手动 4 连发 | 左管严格串行（T1→T2→T3→T4 各 ~60-90s 循环）；玩家嫌慢可切 MANUAL（双管全归玩家）|

### 玩家与程序的双队列语义

- 玩家的手动任务（T1-T4 / Numpad 1-4 / 3D 按钮）入队即 `Registry.Commit`（解析 entityId），雷达 `PickTarget` 跳过已提交目标 → 玩家的 4 连发目标不会被右管抢
- 雷达只给右管派发（OnGunIdle 跳过左管），每次只填右管 1 个槽位
- 玩家的排队任务在雷达 5s 清队列时存活（3a 的保手动语义）
- 切换矩阵：

| 切换 | 左管自动任务 | 右管自动任务 | 手动队列 |
|---|---|---|---|
| AUTO→COOP | 3b 取消/强发 | 继续 | 保留 |
| COOP→AUTO | 玩家任务打完自然交还 | 继续 | 保留 |
| →MANUAL | 3b 处理 | 3b 处理 | 保留 |

---

## Task 1: FSC 模式状态机（核心）

**Files:**
- Modify: `IronNestFCS.Logic/FSC.cs`

- [ ] **Step 1: FcsMode 枚举（`LeftRight` 枚举旁，第 15 行附近）**

```csharp
public enum FcsMode {
    Auto,     // 雷达接管双管
    Coop,     // 雷达右管, 玩家左管
    Manual,   // 雷达休眠, 玩家双管
}
```

- [ ] **Step 2: 模式字段 + 事件 + SetMode（`Mode` 属性 + 3b 副作用）**

```csharp
    /// <summary>当前模式: 默认手动(安全默认)。TryDispatch 按此匹配炮管。</summary>
    public FcsMode Mode { get; private set; } = FcsMode.Manual;

    /// <summary>模式变更事件(FcsModule 做雷达启停, FcsSceneInteractor 刷按钮状态, FcsWindow 刷显示)</summary>
    public event Action<FcsMode>? ModeChanged;

    /// <summary>
    /// 切换模式。调度侧副作用: 清队列(保手动) + 3b 分流——
    /// 失去自动接管权的炮管上的自动任务: 未装填→取消(不碰炮膛), 已装填→forceFire(防卡膛)。
    /// </summary>
    public FcsMode SetMode(FcsMode next)
    {
        ClearPendingTasks();   // 保手动队列(3a)
        if (next == FcsMode.Manual)
        {
            CancelOrForceFire(LeftTask);
            CancelOrForceFire(RightTask);
        }
        else if (next == FcsMode.Coop)
        {
            CancelOrForceFire(LeftTask);   // 左管让位给玩家
        }
        Mode = next;
        ModeChanged?.Invoke(Mode);
        return Mode;
    }

    private static void CancelOrForceFire(ArtilleryTask? task)
    {
        if (task == null || task.Source != TaskSource.Auto) return;
        if (task.progress < Progress.LoadingBullet) task.Canceled = true;
        else task.forceFire = true;
    }
```

> 注：注册表计划 Task 7 Step 4 的 `CancelOrForceFire` 原计划放 `FcsModule`——本计划将其移入 `FSC.SetMode` 统一管理（直接访问 `LeftTask`/`RightTask`）。执行注册表计划时跳过该步，或在协同计划执行时把逻辑迁入 FSC。

- [ ] **Step 3: TryDispatch 重构——CanUseBarrel 炮管匹配**

`TryDispatch` 替换为"找第一个有可用炮管的任务"（保持其余队列顺序）：

```csharp
    /// <summary>任务能否使用指定炮管: 协同模式下手动任务只去左管, 自动任务只去右管; 其余模式任意。</summary>
    private bool CanUseBarrel(ArtilleryTask task, LeftRight barrel)
    {
        if (Mode == FcsMode.Coop)
            return barrel == LeftRight.Right ? task.Source == TaskSource.Auto : task.Source == TaskSource.Manual;
        return true;
    }

    private void TryDispatch() {
        while (_taskQueue.Count > 0) {
            // 找第一个有可用炮管的任务(协同模式下玩家/自动任务按炮管分流)
            ArtilleryTask? chosen = null;
            LeftRight slot = LeftRight.Left;
            foreach (var task in _taskQueue) {
                if (LeftTask == null && CanUseBarrel(task, LeftRight.Left)) { chosen = task; slot = LeftRight.Left; break; }
                if (RightTask == null && CanUseBarrel(task, LeftRight.Right)) { chosen = task; slot = LeftRight.Right; break; }
            }
            if (chosen == null) break;

            // 从队列移除 chosen, 其余保持顺序
            var rebuilt = _taskQueue.Where(t => !ReferenceEquals(t, chosen)).ToArray();
            _taskQueue.Clear();
            foreach (var t in rebuilt) _taskQueue.Enqueue(t);

            if (slot == LeftRight.Left) LeftTask = chosen;
            else RightTask = chosen;
            Registry.Commit(chosen);   // 手动任务幂等(入队已登记)
            StartTaskRoutine(slot, chosen);
        }
    }
```

- [ ] **Step 4: 编译验证**

Run: `dotnet build IronNestFCS.sln -c Release`
Expected: BUILD SUCCEEDED

- [ ] **Step 5: Commit**

```bash
git add IronNestFCS.Logic/FSC.cs
git commit -m "feat: coop mode — FcsMode state machine + barrel-matched dispatch"
```

---

## Task 2: FcsModule 编排

**Files:**
- Modify: `IronNestFCS.Logic/FcsModule.cs`

- [ ] **Step 1: autoMode → fcs.Mode + ModeChanged 订阅**

`Initialize()` 中：

```csharp
        fcs.OnGunIdle += OnGunIdle;
        fcs.ModeChanged += OnModeChanged;
        bool bound = fcs.TryBind();
```

删除 `autoMode` 字段；`OnGunIdle` 守卫改 `if (fcs.Mode == FcsMode.Manual) return;`。新增：

```csharp
    /// <summary>模式变更: 雷达侧副作用。进入 Auto/Coop 立即扫描+派发。</summary>
    private void OnModeChanged(FcsMode mode)
    {
        if (mode != FcsMode.Manual)
            OnGunIdle();
        if (window != null) window.Mode = mode;
    }
```

- [ ] **Step 2: OnGunIdle 协同模式跳过左管**

```csharp
        foreach (var barrel in new[] { LeftRight.Left, LeftRight.Right })
        {
            // 协同模式: 左管永远留给玩家
            if (fcs.Mode == FcsMode.Coop && barrel == LeftRight.Left) continue;
            if (barrel == LeftRight.Left ? fcs.LeftTask != null : fcs.RightTask != null) continue;
            ...
        }
```

- [ ] **Step 3: Update 守卫 + 输入循环**

- 被动扫描块：`if (radar != null && fcs.IsBound && fcs.Mode != FcsMode.Manual && Time.time - lastScanTime > 5f)`
- CBT 轮询块：`if (fcs.IsBound && fcs.Mode != FcsMode.Manual && ...)`
- 模式切换输入（Numpad 0 / Ctrl+0 / Select）：`fcs.SetMode(NextMode(fcs.Mode))`

```csharp
    private static FcsMode NextMode(FcsMode mode) => mode switch
    {
        FcsMode.Auto => FcsMode.Coop,
        FcsMode.Coop => FcsMode.Manual,
        _ => FcsMode.Auto,
    };
```

- [ ] **Step 4: Shutdown 退订**

```csharp
        fcs.OnGunIdle -= OnGunIdle;
        fcs.ModeChanged -= OnModeChanged;
```

- [ ] **Step 5: 编译验证**

Run: `dotnet build IronNestFCS.sln -c Release`
Expected: BUILD SUCCEEDED

- [ ] **Step 6: Commit**

```bash
git add IronNestFCS.Logic/FcsModule.cs
git commit -m "feat: coop mode — radar orchestration (right-barrel only) + mode cycle input"
```

---

## Task 3: FcsSceneInteractor 模式按钮（3D 世界）

**Files:**
- Modify: `IronNestFCS.Logic/FcsSceneInteractor.cs`

- [ ] **Step 1: AutoFire/MaxCharge 之后新增 MODE 按钮**

`InitializeTargetButtons` 的 MaxCharge 按钮段之后、T1-T4 循环之前（同坐标阶梯 x-=0.05 / y-=0.0045）：

```csharp
        // 模式循环按钮: AUTO → COOP → MANUAL。颜色随模式刷新(键盘切换也同步)。
        GameObject modeButton = null;
        modeButton = AddButton(() => {
            fcs.SetMode(NextMode(fcs.Mode));
        }, ModeColor(fcs.Mode));
        modeButton.transform.position = new Vector3(x, y, z);
        modeButton.transform.localScale = Vector3.one * 0.02f;
        var modeText = AddText("MODE: " + fcs.Mode, 14f);
        modeText.transform.SetParent(modeButton.transform, false);
        modeText.transform.localPosition = new Vector3(-1.9f, 0, -10.6f);
        modeText.transform.localScale = Vector3.one * 1.0f;
        fcs.ModeChanged += mode => {
            SetColor(modeButton, ModeColor(mode));
            modeText.GetComponent<Il2CppTMPro.TextMeshPro>().text = "MODE: " + mode;
        };
        x -= 0.05f;
        y -= 0.0045f;
```

辅助方法：

```csharp
    private static FcsMode NextMode(FcsMode mode) => mode switch
    {
        FcsMode.Auto => FcsMode.Coop,
        FcsMode.Coop => FcsMode.Manual,
        _ => FcsMode.Auto,
    };

    private static Color ModeColor(FcsMode mode) => mode switch
    {
        FcsMode.Auto => Color.red,
        FcsMode.Coop => Color.blue,
        _ => Color.white,
    };
```

- [ ] **Step 2: ShutDown 退订**

`ShutDown()` 中 `fcs.ModeChanged` 退订（lambda 保存到字段或使用 `-=` 匿名即可——同一 lambda 引用需保存）。建议：把按钮 lambda 存为字段 `Action<FcsMode>? modeChangedHandler;` 以便退订。

- [ ] **Step 3: 编译验证**

Run: `dotnet build IronNestFCS.sln -c Release`
Expected: BUILD SUCCEEDED

- [ ] **Step 4: Commit**

```bash
git add IronNestFCS.Logic/FcsSceneInteractor.cs
git commit -m "feat: coop mode — 3D mode-cycle button on console"
```

---

## Task 4: FcsWindow 显示

**Files:**
- Modify: `IronNestFCS.Logic/FcsWindow.cs`

- [ ] **Step 1: AutoSweepEnabled → Mode**

```csharp
    public FcsMode Mode { get; set; } = FcsMode.Manual;
```

`[AUTO]` 显示块替换为三态：

```csharp
        GUI.color = Mode == FcsMode.Manual ? ClrLabel : ClrSweep;
        GUI.Label(new Rect(x, y, w, h), Mode switch
        {
            FcsMode.Auto => "[AUTO]",
            FcsMode.Coop => "[COOP]",
            _ => "[MANUAL]",
        });
```

- [ ] **Step 2: COOP 下炮管行归属标注**

`DrawGunRow` 调用处传炮管名：

```csharp
        y = DrawGunRow(Mode == FcsMode.Coop ? "Left (Player)  " : "Left  ", fcs.LeftTask, x, y, w, h, lineH);
        ...
        y = DrawGunRow(Mode == FcsMode.Coop ? "Right (Auto)   " : "Right ", fcs.RightTask, x, y, w, h, lineH);
```

- [ ] **Step 3: 编译验证**

Run: `dotnet build IronNestFCS.sln -c Release`
Expected: BUILD SUCCEEDED

- [ ] **Step 4: Commit**

```bash
git add IronNestFCS.Logic/FcsWindow.cs
git commit -m "feat: coop mode — three-state mode label + barrel ownership in panel"
```

---

## Task 5: 编译验证 + 运行时验证清单（有环境的机器）

- [ ] **Step 1: 全量构建**

```bash
dotnet build IronNestFCS.sln -c Release
```

Expected: BUILD SUCCEEDED。F9 热重载回归：无 ALC 泄漏、无协程残留、模式复位为 MANUAL。

- [ ] **Step 2: 场景 A — COOP 基线**

切 COOP：雷达只打右管（左管 Idle）；玩家 AFK 时左管空闲（火力量减半为预期）；面板显示 `[COOP]`。

- [ ] **Step 3: 场景 B — 玩家开火不被抢**

COOP 下拖标记点 T1 开火：装填+飞行全期雷达不拣该目标（注册表）；左管任务串行执行；T1-T4 连发时左管队列逐个执行，右管并行打自动目标。

- [ ] **Step 4: 场景 C — 切换矩阵**

- AUTO→COOP：左管自动任务按进度取消（面板 Canceled）/强发（已装填），雷达只接管右管
- COOP→AUTO：左管玩家任务原子打完，雷达随后接管左管
- →MANUAL：双管自动任务按进度处理，雷达休眠

- [ ] **Step 5: 场景 D — 模式按钮与快捷键同步**

3D MODE 按钮循环与 Numpad 0/Select 循环状态一致；按钮颜色/文字随任意入口切换刷新。

- [ ] **Step 6: 场景 E — 标记所有权（AutoPlaceMarkers 门控）**

COOP 下雷达扫描后玩家标记不被覆盖（验证 TacticalRadar 门控）；切回 AUTO 恢复覆盖。

- [ ] **Step 7: 场景 F — 炮塔等待**

雷达高吞吐时玩家开火：任务停在 WaitingForFire 属正常，≤1 个射循环（~30s）后开火；面板归属标注可见。

- [ ] **Step 8: 场景 G — 开火序列交替**

双管同时活跃：记录开火序列应为 玩家→程序→玩家→程序 交替；紧相位偶发双发（A,A 或 P,P）后自动恢复交替，无永久偏向。

---

## 风险与已知事项

- **硬依赖**：注册表/3b 计划必须先落地（Source/Registry/Canceled/保手动清队列），本计划在其后执行。
- **AutoPlaceMarkers 门控是正确性关键**：若漏掉，COOP 下雷达每 5s 覆盖玩家标记，手动标点失效。
- **交替是涌现行为**：由锁持有期 + 槽位释放期（13s）自然产生，非设计保证；紧相位偶发双发属预期，自校正。
- **左管空闲 = 火力量减半**：COOP 下玩家 AFK 时自动只有单管火力，为"永远留一管给玩家"的契约代价。
- **模式复位**：热重载后 Mode 复位 MANUAL（安全默认）。
- **下一步可选增强**：若实测玩家等炮塔 >30s 频繁，升级为 3b 抢占（取消未装填自动任务）——本计划默认不做。
